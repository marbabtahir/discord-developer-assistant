# =============================================================================
# Developer Assistant – Discord Bot – Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values. Never commit .env to git.
# See docs/DISCORD_SETUP.md for where to get Discord values.
# =============================================================================

# --- Discord (required) ---
# From Developer Portal → Your App → General Information
APPLICATION_ID=your_application_id

# From Developer Portal → Your App → Bot → Reset Token
BOT_TOKEN=your_bot_token

# From Developer Portal → Your App → General Information → Public Key
PUBLIC_KEY=your_public_key

# --- OpenAI (required for AI features) ---
# From https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key

# --- MongoDB (required for skill scoring & user data) ---
# e.g. mongodb://localhost:27017/developer-assistant or Atlas connection string
MONGODB_URI=your_mongodb_connection_string

# --- GitHub (optional; recommended for /github-analyze and /project-health) ---
# Personal Access Token for higher API rate limit (5000/hr vs 60/hr without).
# See docs/GITHUB_TOKEN.md for how to create one.
GITHUB_TOKEN=ghp_your_token_here

# --- Optional ---
# NODE_ENV=development
# LOG_LEVEL=info
